{
  "private": true,
  "scripts": {
    "compile": "tsc && browserify ./dist/*.js | uglifyjs --compress --mangle - > ../src/dist/bundle.js"
  },
  "dependencies": {
    "browserify": "^13.0.0",
    "typescript": "^1.8.0",
    "uglify-js": "^2.6.1"
  }
}
